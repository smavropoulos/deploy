name: powershell-deploy
description: Execute PowerShell commands via pwsh. Supports single or multi-line scripts via the "command" and "commands" config keys.
entrypoint: deploy-plugin-powershell-deploy
